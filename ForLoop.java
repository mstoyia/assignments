public class ForLoop {
    public static void main(String[] args) {
        // variable type, name, and value
        int sum = 0;
        // declared variable in main method using ForLoop
        for (int i = 1; i <= 5; i++) {
            sum = sum + i;
        }
        System.out.println(sum);
    }
}
